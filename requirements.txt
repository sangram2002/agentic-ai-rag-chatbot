# Force-pin rich first to prevent upgrades
rich==13.7.0
streamlit==1.31.0

# LangChain - Use these specific versions to avoid dependency "bloat"
langchain==0.1.16
langchain-community==0.0.34
langgraph==0.0.39
langchain-core==0.1.45

# Groq and HTTPX - The 'proxies' fix
langchain-groq==0.1.3
groq==0.5.0
httpx==0.27.0

# Embeddings (Must be >= 2.6.0 for langchain-huggingface)
langchain-huggingface==0.0.1
sentence-transformers==2.7.0
faiss-cpu==1.8.0

# Utils
pypdf==4.2.0
python-dotenv==1.0.1